#include <stdio.h>
#include <stdio.h>

/**
 * 재귀 알고리즘
 * 1) 재귀함수는 자기자신을 호출하는 함수이다.
 * 2) 재귀함수는 무한루프에 빠지지 않게 잘 멈추는 것이 중요하다.
 * 
 * - 어떤 값들의 재귀를 사용하여 값을 구할때 합은 초기 세팅을 0 으로 지정하고 곱은 1 로 지정을 해야 한다.
 */

int main() {
    printf("");
}
